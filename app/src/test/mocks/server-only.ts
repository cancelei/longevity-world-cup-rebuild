/**
 * Mock for server-only package in test environment
 * This allows tests to import server-only modules without throwing
 */

// Empty module - just needs to exist to satisfy the import
export {};
